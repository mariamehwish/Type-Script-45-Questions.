function calculate(width: number, height: number): number {
    return width * height;
}

let calculateArrow = (width: number, height: number): number => width * height;

console.log(calculateArrow(5, 7));